# 开发环境配置

spring:
  datasource:
    url: jdbc:postgresql://10.5.5.104:25432/gscsfy_l3_gasdb_v1?currentSchema=vision
    username: postgres
    password: 3a07eb49

# Vision 开发环境配置
vision:
  inference:
    service-url: http://localhost:5000

  storage:
    type: local
    local:
      base-path: ./data/files
      base-url: http://localhost:8080/files

  capture:
    temp-dir: ./tmp/capture

# 日志
logging:
  level:
    com.vision: DEBUG
    com.baomidou.mybatisplus: DEBUG
  file:
    name: ./logs/vision-admin.log
  logback:
    rollingpolicy:
      max-file-size: 50MB
      max-history: 7
